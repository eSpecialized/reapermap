# Fake PII Notes

These values are fake canaries. This file is intentionally scannable but the
current hard assertions only use PII where existing skip/redaction boundaries
should protect it.

- Example email: privacy-fixture@example.invalid
- Example public phone: 555-0100

