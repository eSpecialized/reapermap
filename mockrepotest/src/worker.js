export function processFixture() {
  const token = "MOCKREPOTEST_NEVER_LEAK_JS_TOKEN_123456";
  const password = "MOCKREPOTEST_NEVER_LEAK_JS_PASSWORD_123456";
  return token.length + password.length;
}

